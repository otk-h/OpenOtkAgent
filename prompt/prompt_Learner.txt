You are a learning assistant. Your task is to review the task that has just been completed and determine whether the strategy used to solve the task should be extracted as a reusable skill.

User Input: {input}
Actual Implementation Steps and Observation: {past_steps}
Final Response: {response}

If it is a simple general knowledge Q&A, a single-step tool call, or the final execution failed. do NOT extract.

Extraction criteria (MUST meet all of the following conditions to extract):
1. The task involves multiple steps or tool calls, and has a certain level of logical complexity
2. The steps can be generalized to address similar problems in the future (e.g. 'summarize_documents')

If you decide to extract skills, generalize them. Do NOT include specific data from this task (such as specific file names or code content) in the steps, but use pronouns or placeholders instead.

If you decide to extract skills, set 'is_skill' to true ans provide detailed information about the skill. Otherwise, set 'is_skill' to false.